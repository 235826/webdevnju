# 003：球队信息

> 状态：已验收
>
> 关联事项：Web 开发技术课程大作业

## 目标

用户可以查看球队基础信息，并在比赛相关页面识别参赛球队。

## 用户故事

作为赛事观众，我希望查看球队资料，以便理解比赛双方和赛事参与者。

## 范围

- 球队列表。
- 球队详情。
- 比赛详情中的主客队或对阵双方展示。
- 球队与外部资料来源的关联标识。

## 非目标

- 球员级资料。
- 球队阵容、伤停和转会信息。
- 手动同步外部 API 的完整数据管理流程。

## 业务规则

- **BR-01**：每支球队应具有可展示名称。
- **BR-02**：比赛必须引用统一的球队数据，不在比赛中重复维护独立球队资料。
- **BR-03**：球队详情可以展示本地资料和外部 API 补充资料。
- **BR-04**：外部资料不可用时，球队基础资料仍应可展示。

## Contract 影响

- 结论：新增。
- 理由或影响摘要：需要新增球队读取接口，并供比赛、榜单和外部资料集成引用。
- OpenAPI operation：`GET /api/teams`、`GET /api/teams/{teamId}`。
- 迁移 / 废弃安排：不适用。

## 验收标准

- **AC-01**：给定存在球队数据，当用户打开球队列表时，则可以看到球队名称和基础信息。
- **AC-02**：给定球队详情请求成功，当用户进入球队详情页时，则页面展示该球队的本地资料。
- **AC-03**：给定比赛关联双方球队，当用户查看比赛详情时，则可以识别比赛双方。
- **AC-04**：给定外部球队资料不可用，当用户查看球队详情时，则页面保留本地资料并显示降级提示。
- **AC-05**：给定球队列表为空，当用户打开球队列表时，则页面显示明确空状态。

## 验证映射

| AC    | 验证方式   | 命令或可复现步骤                                                                                           | 结果 / 证据                                                     |
| ----- | ---------- | ---------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| AC-01 | API / E2E  | `npm run test --workspace backend` 查询球队列表；`npm run test:e2e` 打开球队页面                           | 通过：`backend/test/football.test.mts`、`e2e/app-shell.spec.ts` |
| AC-02 | API / E2E  | `npm run test --workspace backend` 查询球队详情；`npm run test:e2e` 打开球队详情页                         | 通过：展示球队本地资料                                          |
| AC-03 | API / E2E  | `npm run test --workspace backend` 检查比赛详情引用统一球队数据；`npm run test:e2e` 从比赛详情进入球队详情 | 通过：比赛双方可识别并可进入球队详情                            |
| AC-04 | 组件 / E2E | `npm run test --workspace frontend` 检查球队详情降级提示；`npm run test:e2e` 打开球队详情                  | 通过：外部资料不可用时保留本地资料并展示提示                    |
| AC-05 | 组件 / E2E | `npm run test --workspace frontend` 检查球队列表空状态文案                                                 | 通过：列表页包含 `暂无球队。` 空状态                            |

## 验收记录

- `npm run check`：通过（需在沙箱外运行，Playwright 测试服务器需要监听本地端口）。
- 人工验收：未单独执行；当前验证由后端 API/服务测试、前端烟测和 Playwright 浏览流程覆盖。
- 已知限制：本切片只提供本地球队资料读取；外部 OpenLigaDB 资料集成由 `specs/009-real-team-api-integration.md` 实现。
