# 005：比赛结果录入

> 状态：已验收
>
> 关联事项：Web 开发技术课程大作业

## 目标

管理员可以录入和修改比赛结果，普通用户不能录入比赛结果。

## 用户故事

作为管理员，我希望维护比赛结果，以便用户查看赛果并驱动积分榜或淘汰赛展示。

## 范围

- 管理员录入比赛结果。
- 管理员修改比赛结果。
- 普通用户和未认证用户禁止录入或修改结果。
- 比赛详情展示结果。
- 比赛结果作为榜单展示的数据来源。

## 非目标

- 自动从第三方数据源同步赛果。
- 复杂赛果审核流程。
- 加时赛、点球大战结果录入。

## 业务规则

- **BR-01**：只有管理员可以录入或修改比赛结果。
- **BR-02**：比赛结果只记录全场主队进球数和客队进球数。
- **BR-03**：管理员可以覆盖已有比赛结果。
- **BR-04**：普通用户和未认证用户的结果写入请求不得改变数据。
- **BR-05**：比赛详情应反映最新有效比赛结果。

## Contract 影响

- 结论：新增。
- 理由或影响摘要：需要新增管理员结果写入接口，并在比赛详情响应中体现结果字段。
- OpenAPI operation：`PUT /api/admin/matches/{matchId}/result`、`GET /api/matches/{matchId}`。
- 迁移 / 废弃安排：不适用。

## 验收标准

- **AC-01**：给定管理员和存在的比赛，当提交合法比赛结果时，则系统保存结果并在比赛详情展示。
- **AC-02**：给定管理员和已有结果的比赛，当提交新的合法结果时，则比赛详情展示更新后的结果。
- **AC-03**：给定普通用户，当提交比赛结果时，则响应为无权限错误且原结果不变。
- **AC-04**：给定未认证用户，当提交比赛结果时，则响应为未认证错误且不写入结果。
- **AC-05**：给定结果输入缺失、非整数或负数，当管理员提交时，则响应为校验错误且不写入结果。

## 验证映射

| AC    | 验证方式  | 命令或可复现步骤                                                                               | 结果 / 证据                                                         |
| ----- | --------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| AC-01 | API / E2E | `npm run test --workspace backend` 管理员保存结果；`npm run test:e2e` 管理员录入后查看比赛详情 | 通过：`backend/test/match-result.test.mts`、`e2e/app-shell.spec.ts` |
| AC-02 | API Test  | `npm run test --workspace backend` 管理员覆盖已有结果并重新查询                                | 通过：比赛详情响应展示新比分                                        |
| AC-03 | API / E2E | `npm run test --workspace backend` 普通用户提交结果；`npm run test:e2e` 普通用户打开比赛详情   | 通过：返回 `FORBIDDEN` / `403`，普通用户不显示结果录入表单          |
| AC-04 | API Test  | `npm run test --workspace backend` 未登录提交结果                                              | 通过：返回 `UNAUTHORIZED` / `401` 且不写入                          |
| AC-05 | API Test  | `npm run test --workspace backend` 管理员提交缺失、非整数和负数比分                            | 通过：返回 `VALIDATION_FAILED` / `400` 且不写入                     |

## 验收记录

- `npm run check`：通过。
- 人工验收：未单独执行；当前验证由后端 API/服务测试、前端烟测和 Playwright 管理员结果录入流程覆盖。
- 已知限制：本切片只保存全场比分并将比赛标记为已结束；积分榜重算、预测结算、排行和淘汰赛晋级自动推进由后续 Spec 实现。
